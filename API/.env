NODE_ENV=dev
PORT=9000
HOST=0.0.0.0

# Caminho para o executável do ffmpeg, se não estiver no PATH do sistema
# FFMPEG_PATH=C:/ffmpeg/bin/ffmpeg.exe

# Diretório base para arquivos temporários (opcional, padrão é os.tmpdir())
# TEMP_DIR_BASE=/var/myapp_temp

# Configurações de tempo em minutos
LINK_EXPIRATION_MINUTES=10
FILE_CLEANUP_INTERVAL_MINUTES=5

FFMPEG_AUDIO_BITRATE=128k
MAX_SEARCH_RESULTS=5

# Domínio(s) do seu frontend para CORS. Separados por vírgula se múltiplos.
# e.g.: CORS_ORIGIN=http://meusite.com,http://meuapp.com
# Para SA-MP, se a requisição vier do servidor SA-MP e não do cliente,
# você pode precisar do IP do servidor SA-MP ou "*" se confiar na sua rede.
CORS_ORIGIN=http://localhost:9000 